package callbackfield

// Deprecated: Legacy factory no longer used after introducing structured callback config.
// Kept as a stub to avoid breaking imports in other packages if referenced.
func NewFieldPrototypes(_ interface{}) ([]FieldPrototype, error) {
	return []FieldPrototype{}, nil
}
